import { CustomedirectiverendererDirective } from './customedirectiverenderer.directive';

describe('CustomedirectiverendererDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomedirectiverendererDirective();
    expect(directive).toBeTruthy();
  });
});

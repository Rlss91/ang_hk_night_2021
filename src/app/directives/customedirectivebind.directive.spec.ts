import { CustomedirectivebindDirective } from './customedirectivebind.directive';

describe('CustomedirectivebindDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomedirectivebindDirective();
    expect(directive).toBeTruthy();
  });
});
